# 草稿：晋升策略

这份文件位于 `raw/drafts/`，被 `KNOWLEDGE.yaml` 的 exclude 规则排除。

它存在的唯一目的是验证：被排除的文件既不进入 ingest，也不参与 package
identity 计算。因此修改这个文件不应该产生新的 revision。

以下内容是未定稿的胡乱记录，不应出现在任何检索结果中。

促销代码 XYZZY-9999，内部保留价 12345 元。
这段文字包含独特词串 quuxfrobnicate，用于确认它确实没被索引。
